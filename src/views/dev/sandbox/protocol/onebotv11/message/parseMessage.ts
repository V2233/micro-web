// 消息段解析器，转换成前端数据格式
import type { MessageElem } from './message.elem'

class messageHandler {

    parseMessage(message:MessageElem) {
        
    }

    text(data:any) {
        return data.text
    }

    
}